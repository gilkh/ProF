// Border Test Component - For debugging theme border issues
import React from 'react';\nimport { getEventTheme } from '@/lib/event-themes';\n\nexport const BorderTest = () => {\n  const eventTheme = getEventTheme('Wedding');\n  \n  return (\n    <div className=\"p-4 space-y-4\">\n      <h2>Border Color Test</h2>\n      \n      {/* Test with inline styles and !important */}\n      <div \n        className=\"p-4 rounded-lg\"\n        style={{\n          border: `2px solid ${eventTheme.primaryColor} !important`,\n          backgroundColor: 'white'\n        }}\n      >\n        Primary Color Border (Completed Task): {eventTheme.primaryColor}\n      </div>\n      \n      <div \n        className=\"p-4 rounded-lg\"\n        style={{\n          border: `2px solid ${eventTheme.accentColor} !important`,\n          backgroundColor: 'white'\n        }}\n      >\n        Accent Color Border (Uncompleted Task): {eventTheme.accentColor}\n      </div>\n      \n      {/* Test with exactly the same classes as task cards */}\n      <div \n        className=\"rounded-xl shadow-lg p-4 space-y-3 transition-all duration-300 hover:shadow-xl relative z-30 bg-background\"\n        style={{\n          border: `2px solid ${eventTheme.primaryColor} !important`,\n          boxShadow: `0 6px 25px ${eventTheme.primaryColor}25`,\n          backgroundColor: 'hsl(var(--background))',\n          position: 'relative',\n          zIndex: 100\n        }}\n      >\n        Exact Task Card Styling Test - Primary\n      </div>\n      \n      <div \n        className=\"rounded-xl shadow-lg p-4 space-y-3 transition-all duration-300 hover:shadow-xl relative z-30 bg-background\"\n        style={{\n          border: `2px solid ${eventTheme.accentColor} !important`,\n          boxShadow: `0 4px 15px ${eventTheme.accentColor}20`,\n          backgroundColor: 'hsl(var(--background))',\n          position: 'relative',\n          zIndex: 100\n        }}\n      >\n        Exact Task Card Styling Test - Accent\n      </div>\n    </div>\n  );\n;